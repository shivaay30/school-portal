<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Rogers School - Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/public/css/styles.css" />
  </head>
  <body class="school-bg-pattern">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-5">
          <div class="card school-login-card shadow">
            <div class="school-hero">
              <img src="https://images.unsplash.com/photo-1580582932707-520b0b3d2f7d?w=80&h=80&fit=crop" alt="School" class="rounded-circle mb-2" width="64" height="64" style="object-fit: cover;" />
              <h1 class="h4 mb-1">Rogers School</h1>
              <p class="mb-0 small">Student & Parent Portal — Sign in</p>
            </div>
            <div class="card-body p-4">
              <% if (error && error.length) { %>
              <div class="alert alert-danger py-2">
                <%= error[0] %>
              </div>
              <% } %>

              <% if (success && success.length) { %>
              <div class="alert alert-success py-2">
                <%= success[0] %>
              </div>
              <% } %>

              <form method="POST" action="/login">
                <div class="mb-3">
                  <label for="email" class="form-label">Email</label>
                  <input
                    type="email"
                    class="form-control"
                    id="email"
                    name="email"
                    placeholder="you@school.com"
                    required
                  />
                </div>
                <div class="mb-3">
                  <label for="password" class="form-label">Password</label>
                  <input
                    type="password"
                    class="form-control"
                    id="password"
                    name="password"
                    placeholder="••••••••"
                    required
                  />
                </div>
                <button type="submit" class="btn btn-primary w-100 fw-semibold">
                  Sign in
                </button>
              </form>

              <p class="mt-3 small text-center mb-0">
                New user? <a href="/signup">Create an account</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
